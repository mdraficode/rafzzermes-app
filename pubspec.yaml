name: rafzzermes_app
description: "Android-only Flutter app for the Rafzzermes App project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.0
  web_socket_channel: ^3.0.0
  http: ^1.4.0
  provider: ^6.1.0
  firebase_messaging: ^15.2.0
  webview_flutter: ^4.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
